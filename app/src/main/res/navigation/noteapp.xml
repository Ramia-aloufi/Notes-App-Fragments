<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/splash"
    app:startDestination="@id/splash2">

    <fragment
        android:id="@+id/splash2"
        android:name="com.example.notesappfragments.splash"
        android:label="fragment_splash"
        tools:layout="@layout/fragment_splash" >
        <action
            android:id="@+id/action_splash2_to_mainview"
            app:destination="@id/mainview" />
    </fragment>
    <fragment
        android:id="@+id/mainview"
        android:name="com.example.notesappfragments.mainview"
        android:label="fragment_mainview"
        tools:layout="@layout/fragment_mainview" />
</navigation>